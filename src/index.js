import React from 'react';
import ReactDOM from 'react-dom/client';

import __rain_func_Router from '@router/index.rain';

const root = ReactDOM.createRoot(document.querySelector("[RAIN_ROOT]"));
root.render(
  <__rain_func_Router />
);
